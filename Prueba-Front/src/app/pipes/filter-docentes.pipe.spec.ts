import { FilterDocentesPipe } from './filter-docentes.pipe';

describe('FilterDocentesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDocentesPipe();
    expect(pipe).toBeTruthy();
  });
});
